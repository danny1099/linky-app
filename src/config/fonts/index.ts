import { Inter, Open_Sans, Comfortaa } from 'next/font/google'

export const globalFont = Inter({ subsets: ['latin'] })
export const brandFont = Comfortaa({ subsets: ['latin'] })
export const titleFont = Open_Sans({ subsets: ['latin'] })
